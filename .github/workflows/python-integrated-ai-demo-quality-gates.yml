name: Python Integrated AI Demo Quality Gates

on:
  push:
    branches:
      - '**'
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: python-integrated-ai-demo-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-python-integrated-ai-workflow:
    name: Validate Python integrated AI workflow
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: Code Files/python/ecommerce-app

    env:
      APP_DIR: Code Files/python/ecommerce-app
      APPLE_PAY_API_KEY: apay_test_demo_key_for_ci_only
      APPLE_PAY_API_URL: https://api.example.com/v1/payments/apple-pay
      STRIPE_API_KEY: stripe_demo_key_for_ci_only

    steps:
      - name: Check out source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: Code Files/python/ecommerce-app/requirements.txt

      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt pytest-cov

      - name: Generate living documentation
        run: |
          set -o pipefail
          mkdir -p reports/logs
          python tools/generate_demo_docs.py 2>&1 | tee reports/logs/01-generate-docs.log

      - name: Validate integrated quality gates
        run: |
          set -o pipefail
          python tools/validate_quality_gates.py 2>&1 | tee reports/logs/02-quality-gates.log

      - name: Compile Python sources
        run: |
          set -o pipefail
          python -m compileall src tests tools 2>&1 | tee reports/logs/03-compile.log

      - name: Run Apple Pay BDD acceptance suite
        run: |
          set -o pipefail
          python -m pytest tests/payment/test_apple_pay.py -v --tb=short 2>&1 | tee reports/logs/04-bdd-tests.log

      - name: Run coverage quality gate
        run: |
          set -o pipefail
          python -m pytest tests/payment/test_apple_pay.py --cov=src.payment --cov-branch --cov-report=term-missing --cov-fail-under=55 2>&1 | tee reports/logs/05-coverage.log

      - name: Generate HTML execution report
        if: always()
        run: |
          python tools/generate_workflow_report.py \
            --run-url "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}" \
            --branch "${{ github.ref_name }}" \
            --sha "${{ github.sha }}" \
            --event "${{ github.event_name }}"

      - name: Publish quality gate summary
        if: always()
        run: |
          if [ -f reports/quality-gates-summary.md ]; then
            cat reports/quality-gates-summary.md >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload workflow evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: python-integrated-ai-quality-gate-evidence
          path: |
            Code Files/python/ecommerce-app/README.md
            Code Files/python/ecommerce-app/docs/generated/integrated-ai-demo-quality-gates.md
            Code Files/python/ecommerce-app/docs/api/apple-pay-openapi.yaml
            Code Files/python/ecommerce-app/reports/quality-gates-summary.md
            Code Files/python/ecommerce-app/reports/quality-gates.json
            Code Files/python/ecommerce-app/reports/integrated-ai-workflow-report.html
            Code Files/python/ecommerce-app/reports/logs/*.log
          if-no-files-found: error

      - name: Commit generated documentation updates
        if: github.event_name == 'push' && github.actor != 'github-actions[bot]'
        working-directory: .
        run: |
          if git diff --quiet -- "$APP_DIR/README.md" "$APP_DIR/docs/generated/integrated-ai-demo-quality-gates.md"; then
            echo "Generated documentation is already committed."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$APP_DIR/README.md" "$APP_DIR/docs/generated/integrated-ai-demo-quality-gates.md"
          git commit -m "docs: refresh Python integrated AI demo documentation"
          git push
